# Wave4.4 Ops Log — Advice Engine Configurable + Rebalance Suggestions

## Changes
- Added advice ruleset tables and default params (rebalance drift, risk bands, behavior weights, max positions).
- Added advice rule registry with fallback behavior.
- Added rebalance suggestions endpoint /api/advice/rebalance.
- Added admin APIs for advice ruleset management.
- Added tests for registry fallback, enable switch, and behavior-weighted scoring.

## Verification
- mvn clean test
- Sample curl:
  - GET /api/advice/rebalance
  - GET /admin/api/advice/rulesets
  - GET /admin/api/advice/rulesets/{code}/params

## Rollback
- git revert <commit_hash> for the 6 Wave4.4 commits (in reverse order).
