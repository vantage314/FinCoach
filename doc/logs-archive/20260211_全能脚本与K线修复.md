# 全能 Python 脚本与 K 线修复 - 日志归档 - 2026-02-11

## 核心成果
1. **全能脚本 `main.py`**:
   - **多线程**: 同时运行行情刷新(3s)、新闻抓取(60s)、F10资料补全(12h)。
   - **F10 增强**: 新增抓取董事长、上市日期、公司官网等详细信息。
   - **稳定性**: 移除 emoji 确保 Windows 兼容，增加异常重试。

2. **后端 K 线修复**:
   - 修正 `MarketController.java`，正确映射前端 K 线周期参数：
     - `day` -> `scale=240`
     - `week` -> `scale=1200`
     - `month` -> `scale=7200`

## 详细变更
### Python
- **文件**: `main.py`
- **功能**:
  - `fetch_sina_market_data`: 解析新浪行情字符串。
  - `task_news`: 请求新浪滚动新闻 API。
  - `process_f10_single`: 解析新浪 F10 网页表格。

### Java
- **文件**: `MarketController.java`
- **逻辑**: 使用 `switch` 语句严格映射 `type` 到 `scale`。

### 数据库
- **更新**: `company_profile` 表新增 `chairman`, `listing_date`, `website` 字段。

## 启动指南
1. **Python**:
   ```bash
   python main.py
   ```
2. **Java**:
   ```powershell
   cd backend
   mvn spring-boot:run
   ```
