# Wave4.2 Ops Log (Crawler Self-Heal) - 2026-02-19

## Changes
- Added stale detection for realtime crawler (heartbeat-based) with idempotent start/stop.
- Added self-heal scheduler (auto-restart on stale) and bounded `last_log`.
- Added realtime health/recover endpoints and admin UI controls.

## Verification (sample)
- `GET /admin/api/data-source/status`
- `GET /admin/api/data-source/realtime/health`
- `POST /admin/api/data-source/realtime/start`
- `POST /admin/api/data-source/realtime/recover`
- `POST /admin/api/data-source/realtime/stop`

Expected highlights:
- `status=RUNNING` with `stale=false` and `secondsSinceHeartbeat` updating
- If stale detected, `recover` or scheduler restarts and `last_end_at` is updated

## Rollback
- Revert commits from Wave4.2 in reverse order if needed.
