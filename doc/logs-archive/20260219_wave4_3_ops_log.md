# Wave4.3 Ops Log (Crawler Observability) - 2026-02-19

## Changes
- Switched last_log truncation to keep tail (newest entries).
- Added recent events ring (max 20) with counters for stale/recover/restart.
- Exposed counters + recent events in status/health and admin UI.

## Verification (sample)
- `GET /admin/api/data-source/status`
- `GET /admin/api/data-source/realtime/health`
- `POST /admin/api/data-source/realtime/recover`

Expected highlights:
- `recentEvents` shows the latest events (tail)
- `staleCount`, `recoverCount`, `restartCount` increment on actions
- `last_log` preserves newest lines

## Rollback
- `git revert <commit>` in reverse order of Wave4.3 commits
