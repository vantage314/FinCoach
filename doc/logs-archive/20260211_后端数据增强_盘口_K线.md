# 后端数据增强与K线修复 - 2026-02-11

## 概述
本次升级补充了 `market_security` 表的五档盘口（bid/ask 1-5）和 `pe_ttm` 字段，重写 Python 脚本完整解析新浪行情接口（索引 0-29），并在前端 `StockDetail.vue` 实现了真实盘口数据的绑定。同时确认后端 K 线接口透传新浪 JSON。

## Step 1: 数据库与实体升级
- SQL: `sql/migration/20260211_add_orderbook_fields.sql`
  - 新增 `bid[1-5]_price` / `bid[1-5]_vol`
  - 新增 `ask[1-5]_price` / `ask[1-5]_vol`
  - 新增 `pe_ttm`
- Java Entity: `MarketSecurity.java` 同步新增字段。

## Step 2: Python 行情采集升级
- 文件: `real_market_updater.py` (v3.1)
- 解析逻辑: 扩展 `fetch_sina_data` 解析索引 10-29（买卖五档量价）。
- 写入逻辑: UPDATE SQL 增加 21 个新字段的写入。

## Step 3: 前端盘口对接
- 文件: `StockDetail.vue`
- 新增 `updateOrderBook` 方法:
  - 将后端返回的 `ask1...ask5` 映射为卖盘列表（卖5在上，卖1在下）。
  - 将 `bid1...bid5` 映射为买盘列表（买1在上，买5在下）。
  - 动态计算挂单量百分比条 (相对于当前最大挂单量)。
- 移除硬编码的模拟数据 (`testOrders`)。

## Step 4: K 线接口确认
- 后端: `MarketController.getKLineData` 透传新浪 API (scale=240,1200...)。
- 前端: `market.ts` 调用后端代理接口。
- 验证: 后端编译通过 (JAVA_HOME=JDK21)。

## 验证与启动
1. **重启 Python 脚本**: `python real_market_updater.py` (确保采集到盘口数据)。
2. **重启后端**: `mvn spring-boot:run` (确保实体字段映射生效)。
3. **前端刷新**: 进入股票详情页，观察五档盘口是否跳动。
