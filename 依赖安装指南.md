# 依赖安装指南

## Python 依赖

### 方法一：使用清华源安装（推荐）
```bash
pip install akshare pandas sqlalchemy pymysql schedule -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 方法二：指定版本安装（如遇冲突）
```bash
pip install --upgrade akshare==1.12.0 pandas==2.0.0 sqlalchemy==2.0.0 pymysql==1.1.0 schedule==1.2.0 -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 方法三：使用 requirements.txt
创建 `requirements.txt` 文件：
```
akshare>=1.12.0
pandas>=2.0.0
sqlalchemy>=2.0.0
pymysql>=1.1.0
schedule>=1.2.0
```

然后安装：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 验证安装
```bash
python -c "import akshare; import pandas; print('安装成功')"
```

## 运行脚本
```bash
python market_updater_final.py
```

## 常见问题

### 1. 提示缺少 lxml
```bash
pip install lxml -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 2. 提示缺少 html5lib
```bash
pip install html5lib -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3. Windows 环境下 pymysql 安装失败
尝试安装 Visual C++ Build Tools 或使用预编译版本。
